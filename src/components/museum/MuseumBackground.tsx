"use client";

/** Decorative background for the artifact viewer: radial gradients + CSS grid lines */
export default function MuseumBackground() {
  return <div className="museum-bg" aria-hidden="true" />;
}
